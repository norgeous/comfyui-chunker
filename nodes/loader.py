# take a path return image(s) tensor
def awesome_loader(path, chunker_config, index):
    # determine if image
        # load image
        # return (tensor, fps=0, total_length=1)
    # determine if video
        # load video chunk
        # return (tensor, fps, total_length)
